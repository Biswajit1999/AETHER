import { createEquationStudy } from '../../shared/equation-engine.js';
import { equationStudies } from '../../shared/equation-catalog.js';
export const createAnimation = runtime => createEquationStudy(runtime, equationStudies.a134);
